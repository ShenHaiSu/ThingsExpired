# 物品过期状态自动更新设计文档

> 本文档描述物品过期状态自动更新功能的技术设计方案，严格遵循 [`项目开发规范.md`](项目开发规范.md) 的全部规范要求。

---

## 目录

1. [需求背景](#1-需求背景)
2. [设计方案](#2-设计方案)
3. [架构设计](#3-架构设计)
4. [详细实现](#4-详细实现)
5. [配置设计](#5-配置设计)
6. [生命周期管理](#6-生命周期管理)
7. [性能与并发](#7-性能与并发)
8. [测试方案](#8-测试方案)
9. [部署注意事项](#9-部署注意事项)

---

## 1. 需求背景

### 1.1 当前问题

物品（Item）有三种状态：

| 状态值 | 常量名 | 说明 |
|--------|--------|------|
| 1 | StatusNormal | 正常 |
| 2 | StatusExpired | 已过期 |
| 3 | StatusUsed | 已消耗 |

**核心问题**：物品在到达过期时间（`expired_at`）后，系统不会自动将状态从 `1`（正常）更新为 `2`（已过期）。

### 1.2 影响范围

1. **统计数据不准确**：`getStats` 接口查询 `status=2` 的物品作为"已过期"，但实际上永远不会自动变成 `2`
2. **用户体验差**：用户需要手动刷新或等待下次查询才能看到过期状态
3. **提醒功能缺失**：虽然有 `remind_days` 字段，但没有实际的自动提醒机制

### 1.3 技术约束

根据 [`项目开发规范.md`](项目开发规范.md) 的规范要求：

1. **严格分层**：定时任务属于 Service 层职责
2. **面向接口**：定义接口，便于测试和替换
3. **配置外置**：定时任务间隔等参数通过配置文件管理
4. **错误统一**：使用统一的 `AppError` 错误处理机制

---

## 2. 设计方案

### 2.1 方案选型

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **方案A：查询时动态判断** | 实现简单，无后台任务 | 每次查询都需要计算，性能差；状态字段失去意义 | ❌ 不采用 |
| **方案B：数据库触发器** | 数据库层面保证 | SQLite 触发器支持有限；违反分层原则 | ❌ 不采用 |
| **方案C：后台定时任务** | 性能好；符合分层原则；可配置 | 需要额外管理定时器生命周期 | ✅ 采用 |

### 2.2 方案C详细说明

采用**后台定时任务**方案，核心思路：

1. 应用启动时，启动一个后台定时器（使用 `setInterval`）
2. 定时触发检查：查询所有 `status=1` 且 `expired_at < 当前时间` 的物品
3. 批量更新这些物品的 `status=2`
4. 记录日志，便于监控
5. 支持优雅关闭（使用 `clearInterval`）

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    应用启动流程                        │
│                                                       │
│  src/index.ts                                         │
│  ├── 加载配置 → 创建 DB → 创建 Repository              │
│  ├── 创建 Service（包括 ItemExpirationService）         │
│  ├── 创建 Handler → 创建 Router → 启动服务器           │
│  └── 调用 expirationService.start() 启动定时任务       │
└─────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│                  定时任务循环                          │
│                                                       │
│  setInterval 触发 → checkExpiredItems()                │
│    → ItemRepository.getExpiredItems() [查询过期物品]   │
│    → ItemRepository.batchUpdateStatus() [批量更新状态]  │
│    → 记录日志                                          │
└─────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│                  分层调用链路                          │
│                                                       │
│  ItemExpirationService (Service层)                     │
│    ↓ 依赖接口                                          │
│  IItemRepository (Repository接口)                      │
│    ↓ 实现                                              │
│  ItemRepository (Repository层)                         │
│    ↓ Drizzle ORM                                       │
│  SQLite 数据库                                         │
└─────────────────────────────────────────────────────┘
```

### 3.2 新增组件

| 组件 | 层级 | 说明 |
|------|------|------|
| [`ItemExpirationService`](../src/service/item_expiration.ts) | Service | 过期检查服务，负责定时任务逻辑 |
| [`IItemExpirationService`](../src/service/interfaces.ts) | Service | 接口定义 |
| `ExpirationConfig` | Config | 定时任务配置 |

---

## 4. 详细实现

### 4.1 Repository 层扩展

#### 4.1.1 接口定义

`IItemRepository` 接口新增以下方法（定义在 [`src/repository/interfaces.ts`](../src/repository/interfaces.ts)）：

```typescript
export interface IItemRepository {
  // ... 现有方法

  /** 获取已过期但状态仍为正常的物品 */
  getExpiredItems(limit: number): Promise<Item[]>;

  /** 批量更新物品状态 */
  batchUpdateStatus(ids: number[], status: number): Promise<void>;
}
```

#### 4.1.2 实现

```typescript
// src/repository/item.ts

import { and, eq, lt, inArray } from 'drizzle-orm';
import { items } from '../model/item';

export class ItemRepository implements IItemRepository {
  constructor(private db: DBInstance) {}

  /**
   * 获取已过期但状态仍为正常的物品
   * 用于后台定时任务检查
   */
  async getExpiredItems(limit: number): Promise<Item[]> {
    const now = new Date().toISOString();

    return await this.db
      .select()
      .from(items)
      .where(
        and(
          eq(items.status, 1),            // 状态为正常
          lt(items.expiredAt, now),        // 已过期
        ),
      )
      .limit(limit)
      .all();
  }

  /**
   * 批量更新物品状态
   */
  async batchUpdateStatus(ids: number[], status: number): Promise<void> {
    if (ids.length === 0) return;

    const now = new Date().toISOString();

    await this.db
      .update(items)
      .set({ status, updatedAt: now })
      .where(inArray(items.id, ids))
      .run();
  }
}
```

### 4.2 Service 层实现

#### 4.2.1 接口定义

```typescript
// src/service/interfaces.ts

/** 物品过期检查服务接口 */
export interface IItemExpirationService {
  /** 启动过期检查任务 */
  start(): void;

  /** 停止过期检查任务 */
  stop(): void;

  /** 执行一次过期检查，返回更新的物品数量 */
  checkExpiredItems(): Promise<number>;
}
```

#### 4.2.2 服务实现

```typescript
// src/service/item_expiration.ts

import type { IItemRepository } from '../repository/interfaces';
import type { IItemExpirationService } from './interfaces';
import type { ExpirationConfig } from '../config';

// 物品状态常量
export const ItemStatus = {
  Normal: 1,
  Expired: 2,
  Used: 3,
} as const;

/**
 * ItemExpirationService 物品过期检查服务
 *
 * 遵循分层架构原则，属于 Service 层组件。
 * 使用 setInterval 实现定时任务，支持优雅启动/停止。
 */
export class ItemExpirationService implements IItemExpirationService {
  private intervalId: ReturnType<typeof setInterval> | null = null;
  private running = false;

  constructor(
    private itemRepo: IItemRepository,
    private config: ExpirationConfig,
    private logger: Console | PinoLogger,
  ) {
    // 设置默认值
    if (this.config.intervalSec <= 0) {
      this.config.intervalSec = 3600; // 默认1小时
    }
    if (this.config.batchSize <= 0) {
      this.config.batchSize = 100; // 默认每次处理100条
    }
  }

  /**
   * 启动过期检查任务
   * 启动时立即执行一次，之后按配置间隔定时执行
   */
  start(): void {
    if (this.running) {
      this.logger.warn('[Expiration] 过期检查任务已在运行中');
      return;
    }

    if (!this.config.enabled) {
      this.logger.info('[Expiration] 过期检查任务已禁用');
      return;
    }

    this.running = true;

    // 启动时先执行一次
    this.checkExpiredItems().catch((err) => {
      this.logger.error('[Expiration] 启动时执行过期检查失败:', err);
    });

    // 定时触发
    this.intervalId = setInterval(() => {
      this.checkExpiredItems().catch((err) => {
        this.logger.error('[Expiration] 定时过期检查失败:', err);
      });
    }, this.config.intervalSec * 1000);

    this.logger.info({
      intervalSec: this.config.intervalSec,
      batchSize: this.config.batchSize,
    }, '[Expiration] 过期检查任务已启动');
  }

  /**
   * 执行一次过期检查
   * @returns 更新的物品数量
   */
  async checkExpiredItems(): Promise<number> {
    // 1. 获取已过期但状态仍为正常的物品
    const expiredItems = await this.itemRepo.getExpiredItems(this.config.batchSize);

    if (expiredItems.length === 0) {
      return 0;
    }

    // 2. 收集需要更新的 ID
    const ids = expiredItems.map((item) => item.id);

    // 3. 批量更新状态为已过期
    await this.itemRepo.batchUpdateStatus(ids, ItemStatus.Expired);

    this.logger.info({ updatedCount: expiredItems.length }, '[Expiration] 过期检查完成');

    return expiredItems.length;
  }

  /**
   * 停止过期检查任务
   */
  stop(): void {
    if (!this.running) {
      return;
    }

    // 清除定时器
    if (this.intervalId !== null) {
      clearInterval(this.intervalId);
      this.intervalId = null;
    }

    this.running = false;
    this.logger.info('[Expiration] 过期检查任务已停止');
  }
}
```

### 4.3 应用入口集成

```typescript
// src/index.ts（关键片段）

async function main() {
  // ... 配置加载、数据库初始化、Repository 创建 ...

  const itemRepo = new ItemRepository(db);

  // 创建过期检查服务
  const expirationService = new ItemExpirationService(
    itemRepo,
    {
      enabled: true,
      intervalSec: 3600,
      batchSize: 100,
    },
    logger,
  );

  // ... Handler、Router 创建 ...

  // 启动定时任务
  expirationService.start();

  // 启动服务器
  Bun.serve({
    fetch: app.fetch,
    port: config.app.port,
    hostname: config.app.host,
  });

  // 优雅关闭
  process.on('SIGINT', () => {
    logger.info('收到 SIGINT 信号，正在关闭服务...');
    expirationService.stop(); // 停止定时任务
    process.exit(0);
  });
}
```

---

## 5. 配置设计

### 5.1 配置项说明

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `expiration.enabled` | boolean | true | 是否启用自动过期检查 |
| `expiration.interval_sec` | number | 3600 | 检查间隔（秒），建议值：300-3600 |
| `expiration.batch_size` | number | 100 | 每次批量处理数量，防止一次性处理过多 |

### 5.2 配置类型定义

```typescript
// src/config/index.ts

export interface ExpirationConfig {
  enabled: boolean;
  intervalSec: number;
  batchSize: number;
}
```

### 5.3 配置建议

| 场景 | intervalSec | batchSize | 说明 |
|------|-------------|-----------|------|
| 开发环境 | 60 | 50 | 快速验证，便于调试 |
| 生产环境（少量物品） | 3600 | 100 | 每小时检查一次 |
| 生产环境（大量物品） | 1800 | 200 | 每30分钟检查，批量处理更多 |

---

## 6. 生命周期管理

### 6.1 启动时序

```
1. main() 被调用
2. 加载配置（含 ExpirationConfig）
3. 初始化数据库连接
4. 创建所有 Repository 实例
5. 创建 ItemExpirationService 实例
6. 创建其他 Service、Handler、Router
7. 调用 expirationService.start() ← 启动定时任务
8. 调用 Bun.serve() ← 启动 HTTP 服务器
```

### 6.2 关闭时序

```
1. 进程收到 SIGINT 信号
2. 调用 expirationService.stop() ← 停止定时任务
3. clearInterval() 清除定时器
4. process.exit(0) ← 退出进程
```

### 6.3 状态保护

```typescript
start(): void {
  if (this.running) {
    return; // 防止重复启动
  }
  // ...
}

stop(): void {
  if (!this.running) {
    return; // 防止重复停止
  }
  // ...
}
```

---

## 7. 性能与并发

### 7.1 性能优化措施

1. **批量处理**：每次最多处理 `batchSize` 条记录，避免一次性处理过多导致数据库压力
2. **索引利用**：`status` 和 `expired_at` 字段需建立索引，确保查询效率
3. **定时间隔**：可配置间隔，根据业务需求调整
4. **异步执行**：定时任务使用 Promise 异步执行，不阻塞主服务

### 7.2 并发安全

| 场景 | 处理方式 |
|------|----------|
| 重复启动 `start()` | `running` 标志位保护，已运行时直接返回 |
| 重复停止 `stop()` | `running` 标志位保护，未运行时直接返回 |
| 同时多次触发 `checkExpiredItems()` | 幂等操作，多次执行结果一致；建议间隔足够大避免重叠 |

### 7.3 资源消耗

| 资源 | 消耗 | 说明 |
|------|------|------|
| CPU | 极低 | 定时触发，大部分时间空闲 |
| 内存 | 极低 | 仅维护一个定时器引用和状态标志 |
| 数据库 | 低 | 每次最多 `batchSize` 条查询和更新 |

---

## 8. 测试方案

### 8.1 单元测试

```typescript
// test/service/item_expiration.test.ts

import { describe, expect, mock, test } from 'bun:test';
import type { IItemRepository } from '../../src/repository/interfaces';
import { ItemExpirationService, ItemStatus } from '../../src/service/item_expiration';

describe('ItemExpirationService', () => {
  test('checkExpiredItems - 有过期物品时应更新状态并返回数量', async () => {
    // 准备 mock
    const mockRepo: IItemRepository = {
      getExpiredItems: mock(() => Promise.resolve([
        { id: 1, status: 1, expiredAt: new Date(Date.now() - 3600000).toISOString() },
        { id: 2, status: 1, expiredAt: new Date(Date.now() - 7200000).toISOString() },
      ])),
      batchUpdateStatus: mock(() => Promise.resolve()),
    } as unknown as IItemRepository;

    const service = new ItemExpirationService(
      mockRepo,
      { enabled: true, intervalSec: 60, batchSize: 100 },
      console,
    );

    // 执行测试
    const count = await service.checkExpiredItems();

    // 断言
    expect(count).toBe(2);
    expect(mockRepo.getExpiredItems).toHaveBeenCalledWith(100);
    expect(mockRepo.batchUpdateStatus).toHaveBeenCalledWith([1, 2], ItemStatus.Expired);
  });

  test('checkExpiredItems - 无过期物品时应返回0', async () => {
    const mockRepo: IItemRepository = {
      getExpiredItems: mock(() => Promise.resolve([])),
      batchUpdateStatus: mock(() => Promise.resolve()),
    } as unknown as IItemRepository;

    const service = new ItemExpirationService(
      mockRepo,
      { enabled: true, intervalSec: 60, batchSize: 100 },
      console,
    );

    const count = await service.checkExpiredItems();
    expect(count).toBe(0);
    expect(mockRepo.batchUpdateStatus).not.toHaveBeenCalled();
  });

  test('start/stop - 启动和停止应正常执行', async () => {
    const mockRepo: IItemRepository = {
      getExpiredItems: mock(() => Promise.resolve([])),
      batchUpdateStatus: mock(() => Promise.resolve()),
    } as unknown as IItemRepository;

    const service = new ItemExpirationService(
      mockRepo,
      { enabled: true, intervalSec: 1, batchSize: 10 },
      console,
    );

    // 启动
    service.start();
    expect(service['running']).toBe(true);

    // 等待一次检查
    await new Promise((resolve) => setTimeout(resolve, 100));

    // 停止
    service.stop();
    expect(service['running']).toBe(false);
  });
});
```

### 8.2 集成测试

```typescript
// test/service/item_expiration.integration.test.ts

import { describe, expect, test, beforeAll, afterAll } from 'bun:test';
import { Database } from 'bun:sqlite';
import { drizzle } from 'drizzle-orm/bun-sqlite';
import * as schema from '../../src/model';
import { ItemRepository } from '../../src/repository/item';
import { ItemExpirationService } from '../../src/service/item_expiration';

describe('ItemExpirationService 集成测试', () => {
  let db: ReturnType<typeof drizzle>;
  let sqlite: Database;

  beforeAll(() => {
    sqlite = new Database(':memory:');
    db = drizzle(sqlite, { schema });

    // 创建表
    db.run(`
      CREATE TABLE items (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        user_id INTEGER NOT NULL,
        category_id INTEGER NOT NULL,
        name TEXT NOT NULL,
        description TEXT,
        quantity INTEGER DEFAULT 1,
        unit TEXT,
        expired_at TEXT NOT NULL,
        remind_days INTEGER DEFAULT 3,
        status INTEGER DEFAULT 1,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL
      )
    `);

    // 插入测试数据
    const now = new Date().toISOString();
    const insert = db.insert(schema.items);
    insert.values([
      { userId: 1, categoryId: 1, name: '已过期物品1', status: 1, expiredAt: new Date(Date.now() - 3600000).toISOString(), createdAt: now, updatedAt: now },
      { userId: 1, categoryId: 1, name: '已过期物品2', status: 1, expiredAt: new Date(Date.now() - 7200000).toISOString(), createdAt: now, updatedAt: now },
      { userId: 1, categoryId: 1, name: '正常物品', status: 1, expiredAt: new Date(Date.now() + 3600000).toISOString(), createdAt: now, updatedAt: now },
    ]).run();
  });

  afterAll(() => {
    sqlite.close();
  });

  test('checkExpiredItems 应更新过期物品状态', async () => {
    const itemRepo = new ItemRepository(db);
    const service = new ItemExpirationService(
      itemRepo,
      { enabled: true, intervalSec: 60, batchSize: 100 },
      console,
    );

    // 执行检查
    const count = await service.checkExpiredItems();

    expect(count).toBe(2); // 应该更新2条

    // 验证状态已更新
    const expiredItems = db.select().from(schema.items)
      .where(eq(schema.items.status, 2)).all();

    expect(expiredItems.length).toBe(2);
    expect(expiredItems[0].name).toBe('已过期物品1');
  });
});
```

### 8.3 测试要求

| 测试类型 | 目标 | 说明 |
|----------|------|------|
| 单元测试 | 覆盖所有核心逻辑 | Mock Repository，测试 Service 行为 |
| 集成测试 | 验证数据库交互 | 使用内存 SQLite 数据库 |
| 边界测试 | 空数据、大量数据 | 确保无过期物品时正常运行 |

---

## 9. 部署注意事项

### 9.1 配置建议

```yaml
# 生产环境配置
expiration:
  enabled: true
  interval_sec: 1800    # 每30分钟检查一次
  batch_size: 200       # 每次最多处理200条
```

### 9.2 监控建议

1. **日志监控**：关注过期检查日志，确认任务正常运行
2. **统计对比**：定期比对 `getStats` 返回的 `expired` 数量与预期
3. **告警设置**：如果连续多次检查失败，应触发告警

### 9.3 多实例部署

| 方案 | 说明 |
|------|------|
| 单实例运行 | 当前设计，简单可靠 |
| 分布式锁 | 多实例时需要引入分布式锁，避免重复检查 |
| 主备模式 | 只有一个实例运行定时任务，其他实例仅提供 API |

**当前项目建议**：单实例部署，无需额外处理。SQLite 不适合多实例写并发。

---

## 附录

### A. 完整流程图

```
序列图: 物品过期检查流程

应用入口                   ItemExpirationService          ItemRepository     SQLite
   |                              |                            |                |
   |--- main() 启动               |                            |                |
   |--- expirationService.start() |                            |                |
   |                              |--- setInterval 定时器       |                |
   |                              |                            |                |
   |                              |=== 定时触发 ===            |                |
   |                              |--- checkExpiredItems()      |                |
   |                              |    |--- getExpiredItems() --|-- SELECT ----> |
   |                              |    |                       |<--- 结果 ------ |
   |                              |    |--- batchUpdateStatus() |-- UPDATE ----> |
   |                              |    |                       |<--- 成功 ------ |
   |                              |--- 记录日志                 |                |
   |                              |                            |                |
   |=== 优雅关闭 ===              |                            |                |
   |--- SIGINT 信号               |                            |                |
   |--- expirationService.stop()  |                            |                |
   |                              |--- clearInterval()          |                |
```

### B. 状态流转图

```
状态机: 物品状态流转

    ┌──────────┐     定时任务自动更新      ┌──────────┐
    │  正常    │ ──────────────────────→ │  已过期  │
    │ (status  │      expired_at < now    │ (status  │
    │  = 1)    │                          │  = 2)    │
    └────┬─────┘                          └────┬─────┘
         │                                    │
         │ 用户手动标记                        │ 用户手动标记
         ▼                                    ▼
    ┌──────────────────────────────────────────────┐
    │                  已消耗                       │
    │              (status = 3)                     │
    └──────────────────────────────────────────────┘

所有状态均可删除（直接物理删除）
```

### C. 文件变更清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| [`src/config/index.ts`](../src/config/index.ts) | 修改 | 添加 `ExpirationConfig` 接口 |
| [`src/repository/interfaces.ts`](../src/repository/interfaces.ts) | 修改 | 添加 `getExpiredItems`、`batchUpdateStatus` 方法 |
| [`src/repository/item.ts`](../src/repository/item.ts) | 修改 | 实现新增的 Repository 方法 |
| [`src/service/interfaces.ts`](../src/service/interfaces.ts) | 修改 | 添加 `IItemExpirationService` 接口 |
| [`src/service/item_expiration.ts`](../src/service/item_expiration.ts) | 新增 | 过期检查服务实现 |
| [`test/service/item_expiration.test.ts`](../test/service/item_expiration.test.ts) | 新增 | 单元测试 |
| [`test/service/item_expiration.integration.test.ts`](../test/service/item_expiration.integration.test.ts) | 新增 | 集成测试 |
| [`src/index.ts`](../src/index.ts) | 修改 | 添加定时任务启动和优雅关闭逻辑 |

---

> 本文档严格遵循 [`项目开发规范.md`](项目开发规范.md) 的全部规范要求。
> 所有实现代码必须与本文档设计保持一致。
